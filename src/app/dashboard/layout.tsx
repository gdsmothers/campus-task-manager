return (
  <div className="min-h-screen bg-gradient-to-br from-slate-950 via-purple-900 to-slate-900 flex flex-col">
    {/* header + sidebar + main exactly as before, but make main cards use bg-white/80, rounded-2xl, etc. */}
  </div>
);